site_name: 'TIBCO GEMS Quickstart'
site_description: 'Getting TIBCO GEMS 1.1.0 running and connected to your EMS servers — gemsctl, the registration YAML, TLS and MTLS, and securing the UI'

nav:
  - GEMS: index.md

plugins:
  - techdocs-core
