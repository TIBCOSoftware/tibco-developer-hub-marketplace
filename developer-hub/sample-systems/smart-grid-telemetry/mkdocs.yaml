site_name: Smart Grid Telemetry
site_description: >-
  Energy & utilities edge-to-cloud IoT telemetry pipeline — smart meters bridged
  to TIBCO EMS, analytics, outage management and billing apps.
docs_dir: docs
site_dir: site
nav:
  - Overview: index.md
theme:
  name: material
  features:
    - navigation.tabs
    - navigation.sections
    - navigation.expand
    - navigation.indexes
    - navigation.top
    - content.code.annotate
    - search.highlight
    - search.suggest
plugins:
  - techdocs-core